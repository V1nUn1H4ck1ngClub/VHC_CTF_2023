version: "3.7"
services:
  app:
    # remember to change the container_name and image name. 
    container_name: 0xr
    image: 0xr:latest
    ports:
      - "13370:13370"
    restart: unless-stopped
